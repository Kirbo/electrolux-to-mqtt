# Migration Guide

This document collects upgrade notes for breaking changes between major versions of `electrolux-to-mqtt`.

Conventions:

- A breaking change is any commit whose Conventional Commit type carries the `!` marker (e.g. `feat!:`, `fix!:`, `refactor!:`). See `.claude/CLAUDE.md` § Tooling.
- Each section below covers one major version. Within a section, list the user-visible breaks and the steps to adapt.
- For non-breaking changes, see [`CHANGELOG.md`](./CHANGELOG.md) (generated by semantic-release on each merge to `main`).

---

## Unreleased

### Removed
- **Token persistence** — OAuth tokens are now memory-only; the service re-authenticates on every startup. Action: delete any existing `tokens.json` from disk (it will be ignored); ensure the Electrolux API is reachable on container start. Any `accessToken`, `refreshToken`, `eat`, or `iat` entries under `electrolux:` in your `config.yml` are silently ignored — remove them.

<!--
When adding a breaking change to this file, follow this template:

## v2.0.0 — YYYY-MM-DD

### Removed
- `<feature>` — replaced by `<replacement>`. Action: <what users must do>.

### Changed
- `<config option>` — semantics changed from X to Y. Action: <what users must do>.

### Renamed
- `<old name>` → `<new name>`.

Refer to commit `<sha>` for the rationale.
-->
